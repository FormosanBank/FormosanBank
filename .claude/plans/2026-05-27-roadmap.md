# FormosanBank near-term roadmap

**Date:** 2026-05-27 (last audited 2026-05-30)
**Status:** Active — A complete; B partially complete (Phases 1-7 + 4.5 of validator refactor landed; B5 cleaner extensions plan written, execution paused for review); C/D/E mostly not started.

**How to read the status markers** (added 2026-05-30 audit pass):
- `[DONE]` — implementation landed and verified. Includes one-line evidence (commit SHA or test count).
- `[PARTIAL]` — some sub-items landed, others outstanding. Explains which is which.
- `[NOT STARTED]` — no implementation work yet.
- `[SUPERSEDED]` — original framing no longer applies; replaced by other work.
- `[BLOCKED]` — depends on something else not yet done.

When an item lands, update its marker in the same commit as the work itself.

## Purpose

Sketch-depth plan covering five near-term initiatives. Each section names a concrete goal, the relevant current-state facts (from investigation), a recommended approach at sketch depth, dependencies on other sub-projects, rough sizing, and the open questions that need answering before drilling in.

The five sub-projects are independent enough that each deserves its own dedicated design pass. This document is the map, not the territory.

## Sub-projects at a glance

| # | Sub-project | Depends on | Recommended phase |
|---|---|---|---|
| A | Python test infrastructure | — | Phase 1 |
| B | XML data tests as CI gates | A | Phase 2 |
| C | PR review system + Hunter S backlog | A+B helpful, not required for backlog | Phase 2 (backlog can start sooner) |
| D | Gitbook sync automation | — | Parallel track, can run anytime |
| E | Claude Code project tooling | — | Phase 0 (multiplies leverage on A–D) |

Recommended sequencing: **E → A → B → C**, with **D** as a parallel track. The Hunter S backlog review under C can start in Phase 0 if it's blocking you, since manual review doesn't require new infrastructure — but it benefits hugely from A+B being in place first.

---

## A. Python test infrastructure

**Status (2026-05-30): [DONE].** Sub-project A landed across commits `cf8312f59` and earlier on `feature/claude-tooling-phase-0`. Test suite now at 124 passed + 19 xfailed (all 19 remaining xfails are cleaner tests scoped to Phase B5). pytest infrastructure, conftest, fixtures, ~120 tests across validators/cleaners/utilities/metrics buckets, plus the tests CI workflow. Two production bugs caught and fixed (`standardize.py` silent-pass on missing-original tier; `clean_xml.py` dropping the XML declaration).

**Goal.** Establish a real test runner, conftest, and the first round of unit tests for the highest-risk QC code, with discovery wired into CI so failures block merge.

**Current state.**
- Zero test infrastructure: no `pytest.ini`, no `pyproject.toml`, no `conftest.py`, no `tests/` tree.
- One genuine unit test exists ([QC/test_find_duplicate_sentences.py](QC/test_find_duplicate_sentences.py)) but uses a hand-rolled PASS/FAIL counter, not pytest, and isn't auto-discovered.
- Two corpus-specific scripts named `test_*.py` under [Corpora/YeddaPalemeqBlog/CodeAndDocs/Scripts/](Corpora/YeddaPalemeqBlog/CodeAndDocs/Scripts/) are baseline analyzers, not unit tests — they should probably be renamed to avoid confusion with real tests.
- `requirements.txt` has no test deps (no pytest, hypothesis, coverage).

**Working assumption.** A non-trivial part of this sub-project IS figuring out what good tests look like for this codebase — we won't know the full set up front. The infrastructure should make it easy to *grow* the suite as we discover what to test, not just stand up a one-shot framework.

**Sketch approach.**
1. [DONE] Add `pytest` + `pytest-cov` (and possibly `hypothesis`) to `requirements.txt` or a separate `requirements-dev.txt`. Pytest + pytest-cov in `requirements.txt`; hypothesis not added (not yet needed).
2. [DONE] Create top-level `tests/` directory with `conftest.py` providing fixtures: a minimal valid `<TEXT>` XML, a temp-corpus directory factory, a path to `QC/validation/xml_template.dtd`. Plus `tests/_helpers.py` for shared marker-matching helpers.
3. [DONE] Establish a `tests/fixtures/` directory of **intentional-problem XML files** (negative test cases). ~50 fixtures covering v###_*.xml (validation negatives), c###_*.xml (cleaner negatives), and valid_*.xml (positives). Grows continuously.
4. [DONE] Migrate `QC/test_find_duplicate_sentences.py` to pytest style as a worked example.
5. [DONE] Add a third GitHub Actions workflow `tests.yaml` that runs `pytest` on every PR and push. Fail the build on test failure. Triggers on QC/**, tests/**, Corpora/**.
6. [DONE] First targeted test additions, ranked by risk: `QC/utilities/standardize.py`, `QC/cleaning/clean_xml.py`, `QC/count_tokens.py`, `QC/corpus_metrics.py`. clean_nonlatin.py not covered (deprecated; deleted in B5 mechanical cleanup).
7. [DONE] Establish a written "saw a problem → wrote a regression test" workflow. Documented in `tests/README.md`.

**Standing instruction (continuous).** Treat test-suite growth as an ongoing activity, not a one-shot setup. I should proactively suggest additional tests over time — especially when reviewing diffs, when running into recurring problem patterns, when discovering invariants in B's discovery work, and when reviewing Hunter S's backlog. Each surfacing of a class of problem is a candidate for a new fixture + test.

**Claude Code support.** A `skill` for "add a test for QC script X" that knows the project's fixture conventions. A `skill` for "turn this bug into a regression test" that captures the discipline from item 7. The `pyright-lsp` plugin (already installed, verified working) gives type-checking for free during test writing.

**Dependencies.** None for the infrastructure itself. B's discovery work (Phase B1 below) feeds A with test ideas.

**Rough sizing.** 1 design session, 1 implementation session for infrastructure + first ~5 tests. Subsequent tests are incremental and continuous.

**Open questions for detailed design.**
- Do you want a separate `requirements-dev.txt` or just add to `requirements.txt`?
- Test layout: `tests/test_<module>.py` mirroring `QC/<module>.py`, or by area (`tests/test_validation/`, `tests/test_cleaning/`)?
- Should the existing `QC/test_find_duplicate_sentences.py` be moved to `tests/` (cleaner) or stay where it is (touches less)?
- Coverage threshold to enforce on CI, or just report?
- For the fixtures: store them as standalone XML files in `tests/fixtures/`, or generate them inline in tests via Python helpers? (Standalone is easier to inspect; inline is easier to diff against the test that uses it.)
- For the "saw a problem → regression test" workflow: should the fixture+test ideally land in the same PR that fixes the bug, or in a follow-up PR? (Same-PR is best for discipline; follow-up is more flexible.)

**Post-A review follow-ups (forward-looking, low priority).** From the 2026-05-30 review of sub-project A:
- [NOT STARTED] **Add a Python version matrix to `tests.yaml`** if the project ever supports >1 Python version simultaneously (today: 3.13 only, sufficient).
- [NOT STARTED] **Empirically document the C012 lookup data in test docstrings.** The C012 tests' "Bunun and Thao preserve, others strip" expectation depends on runtime lookup of `Orthographies/Ortho113/<Language>.tsv`. A B-implementer reading just the test file should be able to re-verify the language list without reading the design doc. Verified empirically (2026-05-30): `-` appears as a letter row in `Bunun.tsv` and `Thao.tsv` only.
- [SUPERSEDED] **Add a "what xfail means here" note to `test_V081_*`.** V081 has now landed (commit `2ddc96670`), so the note is moot — the test passes.
- [NOT STARTED] **Long-term: an `xfail_b_phase` decorator** that wraps the xfail marker and asserts the reason string references a tracked B roadmap item. Makes "what B owes" greppable. Defer until xfail count grows or B work begins. The remaining 19 xfails are all `tests/cleaners/*.py` (all "scope-B" already); decorator gives marginal value at this size.

---

## B. XML data tests as CI gates

**Status (2026-05-30): [PARTIAL].** Major progress across the validator and cleaner work this session:
- **B1 (Discovery):** [PARTIAL]. Items 1, 4 substantially done by sub-project A's design doc (V001–V083 spec is the inventory; DTD→XSD migration reconciled most drift). Item 6 (ISO 639-3 audit) in progress (subagent computing now; will land at `.claude/plans/2026-05-30-b1-iso-639-3-audit.md`). Items 2, 3, 5 NOT started.
- **B2 (Hard-gating infrastructure):** [PARTIAL]. Validator refactor (Phases 1-7) made exit-1-on-HARD the default with `--no-exit-on-hard` opt-out (commit `9d058813f`). xml-validation.yaml CI workflow NOT yet created.
- **B3 (New invariant tests):** [SUPERSEDED / PARTIAL]. The validator rules (rules/hard.py: ~25 functions; rules/soft.py: 2 functions; CROSS_FILE_RULES: V081) are the per-file invariant tests. Corpus-wide invariants (cross-tier comparison, statistical sanity) remain — these need B4 calibration data to be meaningful. See "B3 framing" in this section.
- **B4 (Threshold calibration):** [NOT STARTED]. Blocked on running validators across full `Corpora/` to observe distributions.
- **B5 (Cleaner extensions):** [DONE except item 17]. Mechanical cleanups (items 18, 21, 22 — commits `3b284c62a`, `836a7dcc0`, `af95b626c`). Plan + execution at `.claude/plans/2026-05-30-b5-cleaner-extensions-implementation-plan.md`. Tasks 1-4 (infrastructure: `_get_xml_lang`/`_is_chinese`/CleanerWarnings/TransformCounter + CLI flags): commits `f4deb276d`, `6fae30ded`, `2cde7f753`, `e798a9141`. Tasks 5-8 (rule implementations): `90921cd5d` (C007 Bopomofo), `816b70af3` (C001/C002/C002b + C006 caret), `db492ac28` (C012 hyphens), `30087090b` (C022 asterisk). Items 19 (validator-territory audit; no work needed), 20 (OQ9 docstrings; folded into Task 6), 23 (C006 caret; folded into Task 6) also done. Item 17 (existing-corpora remediation for Chinese punctuation): NOT started — separate Corpora-touching effort.
- **B6 (Category 6 candidates):** [NOT STARTED].
- **B7 (Orthography extraction escape handling):** [NOT STARTED].

**Goal.** Turn the existing manual `validate_*.py` scripts into PR-blocking checks, plus add invariant assertions the current scripts don't cover, plus extend `clean_xml.py` with language-aware behavior. Crucially: *figure out what the right invariants are* by reading the canonical spec, comparing existing corpora, and reading the reproducibility code.

**Design docs (sub-project A produced these during test-writing; they are B's primary specs):**
- [2026-05-29-xml-validation-design.md](2026-05-29-xml-validation-design.md) — every check the new `validate_xml.py` should perform (V001–V083). The xfail tests in `tests/validators/test_validate_xml.py` target the rules not yet enforced; B's validator work flips them to passing.
- [2026-05-29-clean-xml-extension-tests-design.md](2026-05-29-clean-xml-extension-tests-design.md) — the language-aware cleaning behaviors `clean_xml.py` should grow (C001–C026). The "Deferred to sub-project B" section enumerates the six pieces of new cleaner infrastructure needed (language-awareness, warning+CSV output, canonical-orthography lookup, `--hard-remove-segmentation` flag, transformation counter output, corpus remediation).

**Canonical reference.** The canonical XML spec now lives in the gitbook at [`en-us/the-bank-architecture/formosanbank-xml-format.md`](../../FormosanBankGitbook/en-us/the-bank-architecture/formosanbank-xml-format.md). The repo's DTD at [QC/validation/xml_template.dtd](QC/validation/xml_template.dtd) and this prose spec have **already drifted**. Examples found in a quick read of the spec:
- Spec describes a `<PHON>` element (IPA transliterations, with `kindOf` semantics paralleling `<FORM>`); DTD has no `PHON` at all.
- DTD allows `class`/`sclass` on `<W>` and `<M>` and `ver` on `<TRANSL>`; spec doesn't document these (the spec has a literal "didn't include..." note acknowledging the gap).
- Spec mandates `<AUDIO>` `start` and `end` "must always be set"; DTD declares them `IMPLIED` (optional).
- Spec defines multi-citation delimiters (`|` for `citation`, `,` for `BibTeX_citation`) and the `audio="segmented"` convention; DTD enforces neither.

Reconciling these is part of B. Decision needed per disagreement: which is authoritative, and update the other.

**Downstream consumers.** FormosanBank XML is consumed by at least one external tool: the [Formosan-MT-Toolkit](https://github.com/FormosanBank/Formosan-MT-Toolkit) (documented in the gitbook on `origin/en-us` only — PR #20 authored by Hunter S on 2026-05-17, not yet merged to `main`). Its pipeline (`fetch_xml.py → clean_xml.py → make_corpus.py → filter_split_corpus.py`) depends on specific XML invariants — most notably that `<TRANSL>` elements carry well-formed `xml:lang` and `kindOf` attributes that distinguish translation source/type for parallel-pair extraction. **B's invariant tests should explicitly cover what downstream consumers need, not only what the prose spec mandates in the abstract.**

The MT toolkit also has its own `clean_xml.py` (same filename as `QC/cleaning/clean_xml.py` here). The doc says it "runs the standard XML cleaning utilities" but it's ambiguous whether it imports FormosanBank's version or has a copy. This is a duplicate/drift hazard worth reconciling — added to Phase B1 below.

A second locally-present repo, `FormosanChineseMT/`, may be the same project at an earlier stage or a separate predecessor — unclear without further investigation. Worth resolving before B touches anything MT-toolkit-shaped.

**Current state.**
- 7 validation scripts in [QC/validation/](QC/validation/) — all CLIs that emit logs and **never `sys.exit(1)`**. Purely informational.
- Both existing CI workflows ([corpus-metrics.yaml](.github/workflows/corpus-metrics.yaml), [token-comparison.yaml](.github/workflows/token-comparison.yaml)) are also purely informational.
- DTD validation is never run in CI; broken XML can land on `main`.
- Many invariants from the prose spec are not asserted in code anywhere (audio `start`/`end` always set; `audio="segmented"` convention; W-level `standard` retains morpheme segmentation; multi-citation delimiters; infix `-x-` formatting; clitic `=` convention; etc.).

**Sketch approach.** Four phases, B1 (discovery) can start before A is done.

*Phase B1 — Discovery: figure out what to test.*
1. [DONE] Read the canonical XML spec end-to-end and inventory every assertable rule, with a confidence rating per rule. Done by sub-project A in `2026-05-29-xml-validation-design.md` (V001–V083). Phase 4.5 XSD migration further annotated which rules are DTD/XSD-enforced vs Python.
2. [DONE] **Compare `original` vs `standard` tiers across published corpora**. Discovery report at `.claude/plans/2026-05-31-b1-original-vs-standard-discovery.md` (commit `fac85b55d`). Headline: 99.3% of S elements with both tiers are byte-identical (`standardize --copy`). 4 corpora have real transformations: HundredPaiwanStories (Paiwan ortho), WakelinTexts (Yami segmentation removal), FormosanBankGitBook (Eastern Paiwan vowel lowering), MontgomeryTexts (Amis ortho). Real bug surfaced: FormosanBankGitBook standard contains "open-soorce". Structural anomaly: YeddaPalemeqBlog has standard tier appearing BEFORE original in XML.
3. [DONE] **Read each `CodeAndDocs/README.md` and the reproducibility scripts** they describe. Discovery report at `.claude/plans/2026-05-31-b1-codeanddocs-readme-discovery.md` (commit `7fc9c224a`). Surfaces: 6 corpora with stale/incomplete READMEs, 7 with no reproduction scripts, 3 using `DocsAndCode/` instead of `CodeAndDocs/`, UtrechtManuscriptWordList is a raw XML file (not a directory).
4. [DONE] Reconcile the spec/DTD drift. Phase 4.5 XSD migration (commit `1cd3992fa`) reconciled most differences; the remaining drift is the spec's "elements may appear in any order" vs XSD 1.0's `xs:sequence` requirement (deferred — XSD 1.1's `xs:all` with unbounded cardinality not supported by lxml's libxml2 backend).
5. [NOT STARTED] Investigate the MT toolkit's `clean_xml.py` (and any other downstream tooling). Resolve `Formosan-MT-Toolkit` vs `FormosanChineseMT` identity.
6. [DONE] **ISO 639-3 remediation across published corpora.** Audit (`.claude/plans/2026-05-30-b1-iso-639-3-audit.md`) + V035 allow-list update (commit `fb103ba04`) + per-corpus bulk rewrites (14 commits: `47d3bd09e`..`be23ea907`). Final state: 0 bad xml:lang attrs in Corpora/; 5,860 zh-Hans attrs preserved per the explicit V035 allow-list exception (ISO 639-3 cannot express the Simplified-vs-Traditional Chinese script distinction). Test suite green throughout (130 passed, 19 xfailed).

*Phase B2 — Hard-gating infrastructure.*

5. [DONE — inverted form] Add an `--exit-nonzero-on-findings` (or similar) flag. Implemented as the inverse: exit-1-on-HARD is now the default, opt out with `--no-exit-on-hard` (commit `9d058813f`). Preserves the legacy always-exit-0 behavior for callers that need it.
6. [DONE] New GitHub Actions workflow `.github/workflows/xml-validation.yaml` (commit `bd90e7d40`). Two-track design: (a) `validate-changed-files` runs on PR, blocks the PR if any added/modified XML under `Corpora/*/XML/` fails validation; (b) `validate-baseline` runs on push to main against all of `Corpora/` in informational mode (`--no-exit-on-hard`), uploads stderr log + soft CSV as a 90-day artifact. Lets PR enforcement turn on immediately without waiting for legacy corpus cleanup; the baseline-job artifact tracks longitudinal cleanup progress and can be promoted to blocking once the finding count reaches zero.
7. [NOT STARTED] A second tier of "soft" checks (orthography, vocabulary) that comment on PRs but don't block. Depends on B4 calibration.

*Phase B3 — New invariant tests.*
8. [SUPERSEDED / PARTIAL] New invariant tests. The validator rules in `rules/{hard,soft,warn}.py` ARE the per-file invariant tests for everything in the V001–V083 spec. Each rule corresponds to a positive + negative test in `tests/validators/test_validate_xml.py`. **What's NOT covered**: cross-tier statistical invariants (original vs standard divergence), corpus-wide distribution invariants (outlier S lengths, orthography coverage), and a handful of structural invariants the validator-rule architecture is less natural for (e.g., "W-level standard FORM retains segmentation" is a comparison rule, currently not implemented). Those genuinely need a `tests/test_invariants/` directory at some point, but the work is small and blocked on B4 thresholds.

*Phase B4 — Threshold calibration for soft checks.*

9. [NOT STARTED] The orthography/vocabulary checks emit numbers. Calibration ranges undefined.
10. [NOT STARTED] Calibration method (compute distributions across all published corpora believed to be correctly tagged; deliberately mis-label one to sanity-check the wrong-label range).
11. [NOT STARTED] Bake calibrated thresholds into validators as defaults.

*Phase B5 — Cleaner extensions (per [2026-05-29-clean-xml-extension-tests-design.md](2026-05-29-clean-xml-extension-tests-design.md)).*

The clean_xml extension tests written in A are mostly `xfail` against current behavior. They flip to passing as B implements the infrastructure below. **Implementation plan written at [2026-05-30-b5-cleaner-extensions-implementation-plan.md](2026-05-30-b5-cleaner-extensions-implementation-plan.md); execution paused for user review.**

12. [DONE] **Language-aware cleaning.** Helper `_get_xml_lang()` + `_is_chinese()` in Task 1 (commit `f4deb276d`). Wired into language-aware `clean_trans` in Task 6 (commit `816b70af3`).
13. [DONE] **Warning + CSV output infrastructure.** `CleanerWarnings` class in Task 2 (commit `6fae30ded`); wired into the processing pipeline in Task 4 (commit `e798a9141`). Per-rule rows emitted by C002, C002b, C007, C012, C022.
14. [DONE] **Canonical-orthography TSV lookup at clean time.** Task 7 (commit `db492ac28`). `_hyphen_is_letter()` consults `Orthographies/Ortho113/<Language>.tsv`. Verified empirically: `-` is a letter in `Bunun.tsv` and `Thao.tsv` only.
15. [DONE] **`--hard-remove-segmentation` CLI flag.** Task 4 (commit `e798a9141`); used by C012's branch logic in Task 7.
16. [DONE] **Transformation counter output.** Task 3 (commit `2cde7f753`); wired into `main()` in Task 4.
17. [NOT STARTED] **Existing-corpora remediation (one-off).** Audit `Corpora/**` for Chinese TRANSL elements previously Latin-collapsed; rewrite them. Out of scope for the B5 plan per user direction "don't touch Corpora/". Companion to the V035 ISO 639-3 remediation (B1 item 6, DONE). Now ready to be the next Corpora-touching effort.

18. [DONE] **Delete `remove_nonlatin` from `clean_xml.py`.** Commit `3b284c62a`.

19. [DONE — no work needed] **Audit `clean_xml.py` for validator-territory checks.** Audit confirmed nothing in current `clean_xml.py` is validator-territory. No moves required.

20. [DONE] **OQ9 docstring update.** Folded into Task 6 Step 6b (commit `816b70af3`): docstrings on `clean_text`/`clean_trans` documenting language-aware logic AND the language-agnostic caret normalization.

21. [DONE] **Dead-branch cleanup.** Commit `836a7dcc0`.

22. [DONE] **Double-walk cleanup.** Commit `af95b626c`.

23. [DONE] **C006 caret-variant normalization.** Folded into Task 6 (commit `816b70af3`). Four caret-variant Unicode characters (U+2303, U+2038, U+02C6, U+FF3E) normalize to ASCII `^` in BOTH FORM and TRANSL, regardless of `xml:lang`. C006 fixture + test rewritten to verify the full cross-product (4 variants × FORM tiers × Chinese/non-Chinese TRANSL).

*Phase B6 — Category 6 candidates from clean_xml extension transcripts.*

**Status: [NOT STARTED].** All items below are unresolved candidates; no work has begun. Numbering below has a known bug (restarts at 21, overlapping with B5's 21/22 — preserved as-written to avoid breaking inbound references).

The clean_xml extension design's Category 6 lists patterns Joshua flagged during student-XML reviews that aren't the cleaner's job but ARE real corpus quality issues. Per user direction (2026-05-29), several should be considered for automation somewhere — validators, scrapers, or new tooling.

21. [NOT STARTED] **C015 — Footnote numbers stuck onto morphemes** (e.g. `uwal12` from a footnote leak). Validator candidate: flag suspicious digit-runs adjacent to non-digit text in W/M FORM.
22. [NOT STARTED] **C016 — Out-of-language examples in a corpus** (Japanese/Fongbe/Tagalog in an Amis or Bunun file). Hard problem; likely WARN-level if attempted.
23. [NOT STARTED] **C021 — Multi-word glosses that should be joined with `.`**.
24. [NOT STARTED] **C023 — `/` indicates alternative forms.**
25. [NOT STARTED] **C024 — Parentheses in free translations.** Joshua explicitly said this is too idiosyncratic to generalise. Keep as manual review. But need a validator to flag this for user. 
26. [NOT STARTED] **C025 - Decide what validators we need**
27. [NOT STARTED] **C026 - What happens if find_suplicate_sentences.py finds duplicate sentences?** We will need a cleaner, if we don't have one, to remove the duplicates IF the user desires. And this needs to be an explicit part of the QC pipeline.
 
*Phase B7 — Orthography extraction pipeline (escape handling).*

**Status (2026-05-31): [DONE].** Commit `fac85b55d`. `extract_orthographic_info` in `QC/orthography/orthography_extract.py` now calls `html.unescape(text)` before character counting. Real-world impact at audit time: 2,339 escape-sequence occurrences across NTUFormosanCorpus/Bunun and YeddaPalemeqBlog/Paiwan would have been mis-counted. Three regression tests in `tests/utilities/test_orthography_extract.py`. A latent bug in `extract_orthographic_info` (unconditional `unique_chars.remove(" ")` crashes on space-free input) was noted in the commit but not fixed in scope.

**Claude Code support.** A `skill` "validate corpus X" that runs the right sequence with the right flags. A `skill` "compare original vs standard across corpus Y" for B1 discovery. Possibly a `subagent` "spec-vs-DTD reconciler" that systematically walks every rule in the prose spec and reports DTD coverage gaps. A `hook` running fast checks pre-commit is probably overkill — worth a separate decision.

**Dependencies.** **A** (need pytest infrastructure for Phase B3). Phase B1 (discovery) can start before A is done and produces an inventory that informs both A and B3.

**Rough sizing.** B1 discovery: 1–2 sessions plus continuous as new patterns emerge. B2: 1 design + 1 implementation. B3: incremental, grows with the test suite. B4: 1 design + 1 implementation, plus periodic recalibration.

**Open questions.**
- How to handle legacy corpora that fail current validation? Grandfathered with a deny-list? Marked as `xfail` with TODO? Fixed before gating goes live?
- Should `clean_xml.py`/`clean_nonlatin.py` (in-place mutators) be gated behind something stronger than "diff before committing"?
- When the prose spec and the DTD conflict, which is authoritative? Should we always update both together going forward?
- For threshold calibration (B4): is there a "reference corpus" you trust as a calibration baseline, or should we use all published corpora and look for outliers?
- For B1 step 2 (original vs standard comparison): which corpora are most trusted (and so least likely to surface bugs vs. genuine invariants)? Conversely, which are most suspect — those should be looked at *after* the trusted ones, so we have a baseline of what "correct" transformations look like before we encounter sketchy ones.

---

*Phase B8 — Post-cleaning non-ASCII character audit (pipeline integration).*

**Status (2026-05-31): [NOT STARTED].**

26. [NOT STARTED] **Integrate `QC/cleaning/non_ascii_counts.py` into the QC pipeline as a post-cleaning validation step.** Per user direction (2026-05-31): the script already exists; it identifies and counts non-ASCII characters (excluding Chinese) in `<FORM>` elements and prints them in descending frequency, useful for spotting problematic characters that survived cleaning. Treat it as **validation after cleaning** rather than a cleaning step itself. Specifically:
    - Update the [run-qc-pipeline](.claude/skills/run-qc-pipeline.md) skill to run `non_ascii_counts.py` after `clean_xml.py` (and after the upcoming B5 transformation-counter output) and before the validator runs.
    - Capture its output to `<output_dir>/<phase>_non_ascii_counts.log` (matching the existing skill's log-naming convention).
    - Decide whether the output should ever fail the pipeline (probably not at this stage; it's informational), or just be saved as an artifact for review.
    - For CI: a future PR-blocking variant could fail if the non-ASCII character set drifts unexpectedly (e.g., new categories of character appear), but that needs B4-style calibration first.

---

## C. PR review system + Hunter S backlog

**Status (2026-05-30): [NOT STARTED — but C.1 item 3 used implicitly].** Neither C.1 nor C.2 has started as a structured effort. The `pr-review-toolkit` plugin agents have been used during the validator refactor work (review subagents dispatched for each phase), so C.1 item 3 has informal precedent. No PR template, no CODEOWNERS, no Hunter S backlog review.

This is really two intertwined things: a **forward-looking PR review workflow** (everything that lands on `main` from now on) and a **one-time backlog review** of Hunter's already-merged work.

### C.1 Forward-looking PR review system

**Goal.** Every PR gets a consistent layered review: automated CI (A+B), automated code review by AI agents, optional human review, all guided by a PR template.

**Current state.**
- No PR template, no CODEOWNERS, no required reviewers configured.
- Hunter is the only recent significant contributor besides yourself.
- Two installed plugins map directly: `pr-review-toolkit` (Anthropic, multi-agent: code reviewer, simplifier, comment analyzer, silent-failure-hunter, type-design-analyzer, test analyzer) and `code-review` (Anthropic, default review skill). Plus `coderabbit` (third-party, ships diffs to CodeRabbit's AI service — disabled per our earlier audit for this project).

**Sketch approach.**
1. [NOT STARTED] Add `.github/pull_request_template.md`.
2. [PARTIAL] Enable PR-required CI checks once A+B exist. A's `tests.yaml` workflow exists. B's `xml-validation.yaml` workflow does NOT (B2 item 6).
3. [INFORMAL — used during validator refactor] Define a standard review workflow invoking `pr-review-toolkit:code-reviewer` and related agents. Used ad-hoc during validator refactor (sub-project A review, Phase 1-7 final review, etc.). Not yet codified as a documented workflow.
4. [NOT STARTED] Optional `CODEOWNERS` file marking `Corpora/<Lang>/**`.

**Claude Code support.** Heavy use of existing `pr-review-toolkit` plugin. Possibly a thin custom skill that pre-loads FormosanBank-specific review priorities (e.g., "for any PR touching `Corpora/<Lang>/XML/**`, double-check whether the XML still passes DTD validation and whether any reference orthography needs updating").

**Dependencies.** A+B not strictly required but make the gates real.

**Rough sizing.** 1 design session, 1 implementation session (template + workflow + CODEOWNERS if used).

### C.2 Hunter S backlog review (one-time)

**Status (2026-05-30): [NOT STARTED].**

**Goal.** Get through Hunter's ~45 merged commits with priority on the high-risk areas, without spending months on it.

**What I found.** Hunter M Scheppat (`hunterschep`), 45 commits in two bursts:
- **Nov 2024–Feb 2025** (~28 commits): mass XML cleaning/punctuation, dominated by `Corpora/Wikipedias/` (16,725 file-touch events across 16 languages he doesn't read) and `Corpora/ePark/`.
- **May 15–19 2026** (~17 commits): the QC tooling + CI workflows you've been working from since.

**Suggested review order (highest risk first):**
1. [NOT STARTED] **PR #37 — ePark punctuation normalization** (155 files, 5,927 ins / 6,121 del).
2. [NOT STARTED] **Wikipedia bulk-rewrites** — the three "mass rewrite" commits.
3. [NOT STARTED] **PR #33 — Wikipedia metadata fix** (27 files).
4. [NOT STARTED] **`QC/cleaning/clean_xml.py`** (12 revisions, churn signals).
5. [NOT STARTED] **CI workflows + `corpus_metrics.py` / `count_tokens.py`**.
6. [NOT STARTED] **`origin/cleaning` and `origin/punctuation` branches** — Hunter's unmerged WIP.

**Claude Code support.** This is where subagent dispatch shines. For each review item, dispatch a `pr-review-toolkit:code-reviewer` agent with explicit context about Hunter's profile (good coder, doesn't read the languages, churn signals). Use `pr-review-toolkit:silent-failure-hunter` on the cleaning scripts. The skill `coderabbit:autofix` is explicitly off the table per our earlier audit (third-party data shipping).

**Rough sizing.** 1 design session to choose review depth + verification criteria, then estimate 30–90 minutes per high-priority item — call it 5–10 hours of focused work, spread across however many sessions you want.

**Open questions.**
- Verification depth: do you want me to mechanically diff every changed sentence's standard tier against the original, or focus on spot checks?
- Do you have any native-speaker collaborator who could review the highest-risk Wikipedia/ePark content?
- What's the action if I find regressions? Open a fix PR? Revert? Document and live with it?

---

## D. Gitbook sync automation

**Status (2026-05-30): [NOT STARTED].** Entire sub-project (Layers 0–4) is unstarted. Layer 0 (branch reconciliation) is the prerequisite gate for everything else.

**Goal.** Keep `FormosanBankGitbook` reliably in sync with the actual state of `FormosanBank`. Decide explicitly how to handle the zh-TW / pwn drift problem.

**Current state.**
- `main` branch: last commit April 3 2026 (~8 weeks ago). The earlier investigation only ran `git log` on the local checkout (main) and missed branch activity — see Standing instruction in E.
- **Branch drift inside English itself.** The gitbook has both `main` and `origin/en-us`. `origin/en-us` has ~19 commits ahead of `main` — most are "Merge pull request from FormosanBank/main" but several are en-us-specific additions that never came back to main, notably PR #20 (Hunter, 2026-05-17, "Document Formosan MT Toolkit"). Conversely, your recent XML format update (richer `<PHON>` element, sharp `original`/`standard` semantics, infix/clitic special rules) is on `main` and has NOT propagated to `origin/en-us`, which still has older simpler `kindOf` semantics. Which branch the gitbook actually publishes from is not obvious from a glance; needs confirmation.
- Only existing automation: [update_corpus_stats.py](../FormosanBankGitbook/update_corpus_stats.py), which injects stats tables between `<!-- CORPUS STATS START -->` / `END` markers — **only in en-us**.
- No CI in the gitbook repo at all.
- Stale content (concrete examples):
  - `UtrechtManuscriptWordList` missing from gitbook entirely.
  - en-us "Coming Soon" still advertises `Wilang Yutas Videos` and `NTU Corpus` (both shipped).
  - zh-TW missing 13 of 20 corpus pages; pwn missing 12 of 20.
  - Non-English READMEs hardcode `8,075,594 tokens / 731h 40m`. Current reality (per `statistics/corpus_size_history.csv` row 2026-05-18): `7,365,847 words / 436,431 audio-seconds (~121h)`. Different counting methodology, not just stale.
- Statistics CSVs in the gitbook are dated 2026-04-07 (single batch drop).

**Sketch approach.** Four layers, starting with a branch-reconciliation prerequisite.

*Layer 0 — main↔en-us reconciliation (prerequisite).*
0a. [NOT STARTED] Decide what each branch is for.
0b. [NOT STARTED] Establish the normal flow.
0c. [NOT STARTED] Audit current drift.
0d. [NOT STARTED] Reconcile before running Layer 1 lints.

*Layer 1 — en-us update automation.*
1. [NOT STARTED] GitHub Actions workflow in `FormosanBankGitbook` for stats CSV regeneration.
2. [NOT STARTED] Lint for missing per-corpus pages.
3. [NOT STARTED] Lint for "Coming Soon" entries matching shipped corpora.

*Layer 2 — zh-TW suggested-translation PRs.*
4. [NOT STARTED] Workflow opens draft zh-TW translation PRs.
5. [NOT STARTED] Supersession logic.
6. [NOT STARTED] Translation engine selection (LLM vs generic MT).

*Layer 3 — pwn stays manual.*
7. [NOT STARTED] Banner + missing-page lint output. (No code work; just policy + a small banner addition.)

*Layer 4 — Document data-cleaning conventions (per user direction 2026-05-30).*
8. [BLOCKED on B5] **Document the cleaner's punctuation conversion rules in the gitbook.** Depends on B5's language-aware C001/C002/C002b/C007 + C006 caret rules landing first.
9. [BLOCKED on B5+B6] **Related: document the segmentation hyphen rule (C012) and orthography lookup behavior.**

**Claude Code support.** A `skill` "update gitbook" that runs Layer 1. A `subagent` that handles Layer 2 end-to-end: reads the en-us diff, identifies translation-worthy chunks, drafts zh-TW translations, opens the PR, applies supersession logic. No pwn automation.

**Dependencies.** None on A/B/C/E. Fully parallel track.

**Rough sizing.** Layer 1: 1 design + 1–2 implementation sessions. Layer 2: 1 design + 2 implementation sessions (the supersession logic adds nontrivial state). Layer 3 is a banner + the missing-page lint output already produced by Layer 1.

**Open questions.**
- For Layer 2: trigger on every en-us change, or batched (e.g., a weekly digest PR)? Per-change is more responsive; weekly is less PR noise.
- Layer 2 PR identity: GitHub Action bot, or always require a human (or you, via Claude Code) to kick off the translation step?
- How to handle structural changes in the gitbook (new pages, removed pages, restructures) vs. text-only edits? Structural probably always requires human handling for zh-TW.
- For pwn: is there even occasional Paiwan-speaker availability for reviews, or should we expect the lag to grow indefinitely? (Affects whether a missing-page report is actionable or just a record of debt.)
- Should the gitbook eventually become a sub-directory of FormosanBank rather than a sibling repo? (Kills the cross-repo PR complexity, but is a bigger reorganization.)

---

## E. Claude Code project tooling

**Status (2026-05-30): [PARTIAL].** Several skills landed during sub-project A work and the validator refactor sessions; hooks and most subagents not started. See per-item annotations below.

**Goal.** Build out the FormosanBank-specific skills, hooks, MCPs, and subagent patterns that make the other four sub-projects (and ongoing work) easier and safer.

**Current state.**
- Global setup: `~/.claude/CLAUDE.md` with working-style preferences (set up earlier this session).
- Project setup: `CLAUDE.md` with project conventions (set up + iterated this session).
- 13 plugins installed (audited earlier); 1 disabled for this project (`typescript-lsp`). Recommended disables not yet acted on: `security-guidance` (heavy auto-firing hooks), `coderabbit` (third-party data shipping).
- No FormosanBank-specific skills, hooks, or MCPs exist yet.

**Sketch approach.**

*Skills (highest priority):*
- [DONE] **`port-corpus-in`** — committed during sub-project A. Lives at `.claude/skills/port-corpus-in.md`. Updated 2026-05-31 to use `--no-exit-on-hard` per validator refactor review.
- [NOT STARTED] **`split-monolithic-xml`**.
- [DONE] **`run-qc-pipeline`** — committed during sub-project A. Lives at `.claude/skills/run-qc-pipeline.md`. Updated 2026-05-31 to use `--no-exit-on-hard`.
- [DONE] **`setup-new-dev-repo`** — committed during sub-project A. Standardizes the layout for new dev repos.
- [NOT STARTED] **`update-gitbook`** — wraps D Layer 1; blocked on D Layer 0 (branch reconciliation).

*Hooks:*
- [NOT STARTED] **PreToolUse(Write|Edit) on `statistics/corpus_size_history.csv` and `statistics/corpus_size_over_time.png`** — block direct writes. (A python script exists at `.claude/hooks/test_block_statistics_edits.py` but is not yet wired into settings.json as a hook.)
- [NOT STARTED] **PostToolUse(Bash) matching `python QC/cleaning/clean_xml.py`** — prompt for `git diff` review.
- [DONE] **SessionStart hook validates `.venv` has dependencies installed** — committed during sub-project A as part of the dev experience hardening.

*MCPs:*
- [N/A — installed but not actively used] `github` MCP. Needs `GITHUB_PERSONAL_ACCESS_TOKEN` env var; not exercised in current work.
- [DECIDED] No FormosanBank-specific MCP server.

*Subagents:*
- [DONE — informally used] Lean on existing `pr-review-toolkit` agents. Used during validator refactor sessions for the final A review, the Phase 1-7 final review, etc.
- [NOT STARTED] Custom `corpus-content-reviewer` agent for the Hunter S backlog work.

*Plugin actions:*
- [DONE] Disable `typescript-lsp` for this project (in `.claude/settings.local.json` → `enabledPlugins`).
- [NOT STARTED] Disable `security-guidance` and `coderabbit` per the audit recommendation.
- [NOT STARTED] Investigate `pyright-lsp` plugin install completeness.

**Standing instruction (continuous).** When investigating any repo in the FormosanBank ecosystem (especially the gitbook, which has `main` and `origin/en-us` branches that drift), enumerate branches before concluding "X isn't there." Default `git log` only walks the current HEAD; `git branch -a` and `git log --all --oneline main..origin/<other>` are the minimum checks. Treat "I read main and the file wasn't there" as evidence of absence only after explicitly checking other branches. This lesson came from missing PR #20 (Hunter's MT toolkit doc, on `origin/en-us`) during the initial gitbook investigation.

**Claude Code support.** N/A — this *is* the Claude Code support layer.

**Dependencies.** None. But the value of each item depends on what the other sub-projects look like — e.g., `run-qc-pipeline` is more useful once B exists.

**Rough sizing.** Each skill: 1 design + 1 implementation session. Hooks: half a session each. ~4 skills × 2 sessions + ~3 hooks × 0.5 sessions = ~9–10 sessions to build out, though they can come in over months as needed.

**Open questions.**
- Which skills feel most valuable to you? (My ranked order above is a guess.)
- For the in-place-mutator hooks: too noisy? Or worth the friction?
- Custom subagent (`corpus-content-reviewer`) — useful, or are the existing `pr-review-toolkit` agents sufficient with FormosanBank-specific context in the prompt?

---

## Cross-cutting open questions

**Status (2026-05-30): [OPEN].** All five questions remain unresolved as of this audit.

These don't belong to any single sub-project but affect all of them:

1. [OPEN] **Collaborators.** Is FormosanBank likely to gain other contributors in the next 6–12 months? This affects how much we should invest in formal review machinery (C) and shared conventions (E).
2. [OPEN] **Audio.** Audio-related QC is currently underweighted because of audio's git-lfs/hf complexity. (Note: validator refactor implemented V050/V051/V052/V053/V054 — audio attribute structural checks. The deeper audio-file-existence/duration-matching invariants live in a separate validator.)
3. [OPEN] **Speed.** CI budget per PR? B5 + B6 + B7 add load; B4 calibration runs are slow.
4. [OPEN] **Versioning.** Release/tagging discipline?
5. [OPEN] **The "associated repos" rule.** Read/modify permission for corpus dev repos for E.skills like `port-corpus-in`?

---

## Recommended next step

Pick one sub-project from A–E (or attack C.2 as a tactical side project), and we drill in: clarifying questions → design → spec → implementation plan.
