# Known and accepted findings

There are zero unresolved findings.

- The source uses scholarly transcription (`ð`, `ʃ`, `ɬ`, `θ`, `ŋ`, `ʔ`, accented vowels) rather than the practical reference inventory. Original forms preserve it.
- Sentence-level standard forms remove `-`, `=`, `<`, and `>` segmentation notation. They do not otherwise transliterate the article's scholarly transcription because no reviewed conversion is available.
- W-level standard forms retain the source's analytical segmentation. This preserves exact word/gloss alignment and follows the canonical FormosanBank pattern in which S standards are flattened while W standards retain analytical boundaries.
- Source infix notation (`<…>`) and English parenthetical/slash alternatives produce reviewed text warnings.
- The three footnote examples have no printed gloss line, so they intentionally have no W/M tier. Their three V060 W-count notices are accepted.
- W/M structure for examples (1)–(24) comes only from the printed aligned form/gloss lines. Monomorphemic W nodes have no M children.
